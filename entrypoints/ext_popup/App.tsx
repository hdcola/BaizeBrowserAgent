import React from "react";
import "@/assets/styles/main.css";
// import "./style.css"; // Ensure local style is imported if not already by main.tsx (actually main.tsx likely keeps it or we should add it)
import "./style.css";
import MainApp from "@/components/MainApp";

function App() {
  return <MainApp />;
}

export default App;
